import React from 'react'
import ReactDOM from 'react-dom/client'
import GreetingScreen from './GreetingScreen.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GreetingScreen />
  </React.StrictMode>,
)
